{{-- resources/views/layouts/app.blade.php --}}
{{-- Simple wrapper: if you already have layouts.master this will reuse it --}}
@extends('layouts.master')

{{-- If layouts.master yields 'content', nothing else needed.
   But if your master uses a different section name, adapt accordingly. --}}
